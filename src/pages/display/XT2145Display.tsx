import Home from '../Home';

/**
 * XT2145 HDMI-1 touch surface.
 * HDMI-2 is intentionally not rendered here; autorun owns it as a second
 * independent 1920x1080 HtmlWidget.
 */
export default function XT2145Display() {
  return <Home />;
}
