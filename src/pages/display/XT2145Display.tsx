import Home from '../Home';

/**
 * XT2145 HDMI-1 touch surface (4K HtmlWidget, 1920×1080 design scaled).
 * HDMI-2 is native roVideoPlayer — not rendered here.
 */
export default function XT2145Display() {
  return <Home />;
}
